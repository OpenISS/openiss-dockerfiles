To build the image, run (in the sdkbase directory):
docker build -f Dockerfile.openisssdkbase . -t openisssdkbase

To run the image:
docker run --gpus=all --rm -it -e DISPLAY=<your_ipv4_address>:0.0 openisssdkbase

Run one of the examples using one of the provided images:
./_edetface /usr/src/sdkbase/openiss/openedet/images/biden2.png

For downloading videos from youtube:
apt install python3.7
ln -s /usr/bin/python3 /usr/bin/python3.7

apt-get upgrade python3-pip

pip3 install yt-dlp

yt-dlp <url of youtube video>

then simply run the examples using the path to the mp4 as an argument the same way you would with an image
